server
# Local data and databases (may contain sensitive information)
data/
*.db
*.sqlite
*.sqlite3

# Environment files with secrets
.env
.env.local
.env.*.local

# Test outputs and temporary files
*.log
.DS_Store

# Compiled helper binaries (build with `go build ./tools/...` instead)
/tools/mint_dpop_proof/mint_dpop_proof
/tools/mint_rfc7523_assertion/mint_rfc7523_assertion
/tools/mint_assertion/mint_assertion

# Signing keys — never commit private keys
/keys/

# Internal reference material — must not be committed to a public repo.
# Contains Twilio account identifiers, staging hostnames, and links to
# internal documents; GitHub push protection blocks it, correctly.
/docs/Whitney_Bug_Bash_Repo.md
